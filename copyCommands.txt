let cps = await getCps(page)
let cookies = await getCookies(page)
await clickCookie(page, 19)
let prices = await getPrices(page)
let cookies = await getCookies(page)
await buyBuilding(page, Grandma)
let cps = await getCps(page)
